import required_if from "./requiredIf.js";
import jalali_date from "./jalaliDate.js";
import required from "./required.js";
import national_code_ir from "./nationalCodeIr.js";
import mobile from "./mobile.js";
import email from "./email.js";
import uri from "./uri.js";
import integer from "./integer.js";
import formula from "./formula.js";
import nullable from "./nullable.js";

const validators = {
  required_if,
  jalali_date,
  required,
  national_code_ir,
  mobile,
  email,
  uri,
  integer,
  formula,
  nullable,
};

export {
  required_if,
  jalali_date,
  required,
  national_code_ir,
  mobile,
  email,
  uri,
  integer,
  formula,
  nullable,
};

export default validators;
